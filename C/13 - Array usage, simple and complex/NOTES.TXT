in C 23/C++ 11 and above you can initialize arrays and whole struct members with {}, which initializes the vars to zeroes. So for example:

struct data
{
  int a;
  int b;
  int c;
};

data str;

str{} or str = {}
