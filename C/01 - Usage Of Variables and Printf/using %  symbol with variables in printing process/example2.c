#include <stdio.h>
#include <stdlib.h>

int main()

{

  char character_name[] = "John";
  int character_age = 56;
  char professione_name[] = "Programmer";
  int salary = 1250;

  printf("His name was %s\n", character_name);

  printf("He never liked to be %d\n", character_age);

  printf("but still, he wanted to be a %s\n", professione_name);

  printf("Sadly the place where he was living, wouldn't give that much payday, in fact\nhe was earning only %d€", salary);
  }
