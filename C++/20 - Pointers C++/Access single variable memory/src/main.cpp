//NOTE: works for C too.

#include <iostream>

using namespace std;

int main() {

int *a = (int*)malloc(sizeof(int);
		      
cout << "Insert a number" << endl;
		      
for (size_t i = 0; i < 1; i++) { 
	cin >> *a;
	if (a[i] == 1){
		cout << "Number detected" << endl;
	}
   }
return 0;
}
